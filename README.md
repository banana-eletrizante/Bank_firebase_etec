# Bank Firebase ETEC

Simulação de banco em Ionic + Angular + Firebase (projeto de curso).

```bash
npm install
ionic serve
```

Código em `src/`. Não commite `.env` nem chaves do Firebase — use o Console e restrição de domínio/app.
